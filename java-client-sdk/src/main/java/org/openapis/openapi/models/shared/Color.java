/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package org.openapis.openapi.models.shared;


import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * <p>Wrapper class for an "open" enum. "Open" enums are those that are expected
 * to evolve (particularly with the addition of enum members over time). If an
 * open enum is used then the appearance of unexpected enum values (say in a 
 * response from an updated an API) will not bring about a runtime error thus 
 * ensuring that non-updated client versions can continue to work without error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe
 * cache is maintained to ensure that). As a consequence instances created with the 
 * same value will satisfy reference equality (via {@code ==}).
 * 
 * <p>This class is intended to emulate an enum (in terms of common usage and with 
 * reference equality) but with the ability to carry unknown values. Unfortunately
 * Java does not permit the use of an instance in a switch expression but you can 
 * use the {@code asEnum()} method (after dealing with the `Optional` appropriately).
 *
 */
@JsonDeserialize(using = Color._Deserializer.class)
@JsonSerialize(using = Color._Serializer.class)
public class Color {

    public static final Color RED = new Color("red");
    public static final Color GREEN = new Color("green");
    public static final Color BLUE = new Color("blue");
    public static final Color RETURN = new Color("return");
    public static final Color CLASS = new Color("class");
    public static final Color INT = new Color("int");

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<String, Color> values = createValuesMap();
    private static final Map<String, ColorEnum> enums = createEnumsMap();

    private final String value;

    private Color(String value) {
        this.value = value;
    }

    /**
     * Returns a Color with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as Color
     */ 
    public static Color of(String value) {
        synchronized (Color.class) {
            return values.computeIfAbsent(value, v -> new Color(v));
        }
    }

    public String value() {
        return value;
    }

    public Optional<ColorEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        Color other = (Color) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "Color [value=" + value + "]";
    }

    // return an array just like an enum
    public static Color[] values() {
        synchronized (Color.class) {
            return values.values().toArray(new Color[] {});
        }
    }

    private static final Map<String, Color> createValuesMap() {
        Map<String, Color> map = new LinkedHashMap<>();
        map.put("red", RED);
        map.put("green", GREEN);
        map.put("blue", BLUE);
        map.put("return", RETURN);
        map.put("class", CLASS);
        map.put("int", INT);
        return map;
    }

    private static final Map<String, ColorEnum> createEnumsMap() {
        Map<String, ColorEnum> map = new HashMap<>();
        map.put("red", ColorEnum.RED);
        map.put("green", ColorEnum.GREEN);
        map.put("blue", ColorEnum.BLUE);
        map.put("return", ColorEnum.RETURN);
        map.put("class", ColorEnum.CLASS);
        map.put("int", ColorEnum.INT);
        return map;
    }
    
    @SuppressWarnings("serial")
    public static final class _Serializer extends StdSerializer<Color> {

        protected _Serializer() {
            super(Color.class);
        }

        @Override
        public void serialize(Color value, JsonGenerator g, SerializerProvider provider)
                throws IOException, JsonProcessingException {
            g.writeObject(value.value);
        }
    }

    @SuppressWarnings("serial")
    public static final class _Deserializer extends StdDeserializer<Color> {

        protected _Deserializer() {
            super(Color.class);
        }

        @Override
        public Color deserialize(JsonParser p, DeserializationContext ctxt)
                throws IOException, JacksonException {
            String v = p.readValueAs(new TypeReference<String>() {});
            // use the factory method to ensure we get singletons
            return Color.of(v);
        }
    }
    
    public enum ColorEnum {

        RED("red"),
        GREEN("green"),
        BLUE("blue"),
        RETURN("return"),
        CLASS("class"),
        INT("int"),;

        private final String value;

        private ColorEnum(String value) {
            this.value = value;
        }

        public String value() {
            return value;
        }
    }
}
