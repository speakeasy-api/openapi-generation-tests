/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

package org.openapis.openapi.models.shared;


import com.fasterxml.jackson.core.JacksonException;
import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import java.io.IOException;
import java.lang.Long;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

/**
 * <p>Wrapper class for an "open" enum. "Open" enums are those that are expected
 * to evolve (particularly with the addition of enum members over time). If an
 * open enum is used then the appearance of unexpected enum values (say in a 
 * response from an updated an API) will not bring about a runtime error thus 
 * ensuring that non-updated client versions can continue to work without error.
 *
 * <p>Note that instances are immutable and are singletons (an internal thread-safe
 * cache is maintained to ensure that). As a consequence instances created with the 
 * same value will satisfy reference equality (via {@code ==}).
 * 
 * <p>This class is intended to emulate an enum (in terms of common usage and with 
 * reference equality) but with the ability to carry unknown values. Unfortunately
 * Java does not permit the use of an instance in a switch expression but you can 
 * use the {@code asEnum()} method (after dealing with the `Optional` appropriately).
 *
 */
@JsonDeserialize(using = HeroWidth._Deserializer.class)
@JsonSerialize(using = HeroWidth._Serializer.class)
public class HeroWidth {

    public static final HeroWidth FOUR_HUNDRED_AND_EIGHTY = new HeroWidth(480L);
    public static final HeroWidth SEVEN_HUNDRED_AND_TWENTY = new HeroWidth(720L);
    public static final HeroWidth ONE_THOUSAND_AND_EIGHTY = new HeroWidth(1080L);

    // This map will grow whenever a Color gets created with a new
    // unrecognized value (a potential memory leak if the user is not
    // careful). Keep this field lower case to avoid clashing with
    // generated member names which will always be upper cased (Java
    // convention)
    private static final Map<Long, HeroWidth> values = createValuesMap();
    private static final Map<Long, HeroWidthEnum> enums = createEnumsMap();

    private final long value;

    private HeroWidth(long value) {
        this.value = value;
    }

    /**
     * Returns a HeroWidth with the given value. For a specific value the 
     * returned object will always be a singleton so reference equality 
     * is satisfied when the values are the same.
     * 
     * @param value value to be wrapped as HeroWidth
     */ 
    public static HeroWidth of(long value) {
        synchronized (HeroWidth.class) {
            return values.computeIfAbsent(value, v -> new HeroWidth(v));
        }
    }

    public long value() {
        return value;
    }

    public Optional<HeroWidthEnum> asEnum() {
        return Optional.ofNullable(enums.getOrDefault(value, null));
    }

    public boolean isKnown() {
        return asEnum().isPresent();
    }

    @Override
    public int hashCode() {
        return Objects.hash(value);
    }

    @Override
    public boolean equals(java.lang.Object obj) {
        if (this == obj)
            return true;
        if (obj == null)
            return false;
        if (getClass() != obj.getClass())
            return false;
        HeroWidth other = (HeroWidth) obj;
        return Objects.equals(value, other.value);
    }

    @Override
    public String toString() {
        return "HeroWidth [value=" + value + "]";
    }

    // return an array just like an enum
    public static HeroWidth[] values() {
        synchronized (HeroWidth.class) {
            return values.values().toArray(new HeroWidth[] {});
        }
    }

    private static final Map<Long, HeroWidth> createValuesMap() {
        Map<Long, HeroWidth> map = new LinkedHashMap<>();
        map.put(480L, FOUR_HUNDRED_AND_EIGHTY);
        map.put(720L, SEVEN_HUNDRED_AND_TWENTY);
        map.put(1080L, ONE_THOUSAND_AND_EIGHTY);
        return map;
    }

    private static final Map<Long, HeroWidthEnum> createEnumsMap() {
        Map<Long, HeroWidthEnum> map = new HashMap<>();
        map.put(480L, HeroWidthEnum.FOUR_HUNDRED_AND_EIGHTY);
        map.put(720L, HeroWidthEnum.SEVEN_HUNDRED_AND_TWENTY);
        map.put(1080L, HeroWidthEnum.ONE_THOUSAND_AND_EIGHTY);
        return map;
    }
    
    @SuppressWarnings("serial")
    public static final class _Serializer extends StdSerializer<HeroWidth> {

        protected _Serializer() {
            super(HeroWidth.class);
        }

        @Override
        public void serialize(HeroWidth value, JsonGenerator g, SerializerProvider provider)
                throws IOException, JsonProcessingException {
            g.writeObject(value.value);
        }
    }

    @SuppressWarnings("serial")
    public static final class _Deserializer extends StdDeserializer<HeroWidth> {

        protected _Deserializer() {
            super(HeroWidth.class);
        }

        @Override
        public HeroWidth deserialize(JsonParser p, DeserializationContext ctxt)
                throws IOException, JacksonException {
            long v = p.readValueAs(new TypeReference<Long>() {});
            // use the factory method to ensure we get singletons
            return HeroWidth.of(v);
        }
    }
    
    public enum HeroWidthEnum {

        FOUR_HUNDRED_AND_EIGHTY(480L),
        SEVEN_HUNDRED_AND_TWENTY(720L),
        ONE_THOUSAND_AND_EIGHTY(1080L),;

        private final long value;

        private HeroWidthEnum(long value) {
            this.value = value;
        }

        public long value() {
            return value;
        }
    }
}
