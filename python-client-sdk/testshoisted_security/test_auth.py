"""Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT."""

from sdk.models import operations
from sdk import SDK
from .common_helpers import record_test

def test_no_auth():
    record_test('auth-no-auth')

    s = SDK()
    assert s is not None

    res = s.auth.no_auth()
    assert res is not None
    assert res.status_code == 200


def test_basic_auth():
    record_test('auth-basic-auth')

    s = SDK()
    assert s is not None

    res = s.auth.basic_auth(
        security=operations.BasicAuthSecurity(
            username='testUser',
            password='testPass',
        ),
        passwd='testPass',
        user='testUser'
    )
    assert res is not None
    assert res.status_code == 200
    assert res.user.authenticated == True


def test_api_key_auth_global():
    record_test('auth-api-key-auth-global')

    s = SDK(api_key_auth='Bearer test_token')
    assert s is not None

    res = s.auth.api_key_auth_hoisted()

    assert res is not None
    assert res.status_code == 200


def test_api_key_auth_operation():
    record_test('auth-api-key-auth-operation')

    s = SDK()
    assert s is not None

    res = s.auth.api_key_or_bearer_auth(security=operations.APIKeyOrBearerAuthSecurity(
        api_key_auth='Bearer test_token'
    ))
    assert res is not None
    assert res.status_code == 200
    assert res.token.authenticated is True
    assert res.token.token == 'test_token'
