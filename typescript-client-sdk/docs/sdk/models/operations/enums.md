# Enums


## Supported Types

### `shared.Enum[]`

```typescript
const value: shared.Enum[] = [
  "one",
];
```

### `{ [k: string]: shared.Enum }`

```typescript
const value: { [k: string]: shared.Enum } = {
  "key": "one",
};
```

