/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { Readable } from "stream";
import { createReadStream } from "fs";
import { readFile } from "fs/promises";

export function filesToStream(filePath: string): ReadableStream<Uint8Array> {
    return Readable.toWeb(createReadStream(filePath)) as ReadableStream<Uint8Array>;
}

export async function filesToByteArray(filePath: string): Promise<Buffer> {
    return readFile(filePath);
}

export async function filesToString(filePath: string): Promise<string> {
    return readFile(filePath, "utf8");
}
