/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import jp from "jsonpath";
import { SDKCore } from "../core.js";
import { encodeFormQuery, encodeJSON } from "../lib/encodings.js";
import * as M from "../lib/matchers.js";
import { compactMap } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../sdk/models/errors/httpclienterrors.js";
import { SDKError } from "../sdk/models/errors/sdkerror.js";
import { SDKValidationError } from "../sdk/models/errors/sdkvalidationerror.js";
import * as operations from "../sdk/models/operations/index.js";
import { PaginationAmbiguousInputServerList } from "../sdk/models/operations/paginationambiguousinput.js";
import { Result } from "../sdk/types/fp.js";
import {
  createPageIterator,
  haltIterator,
  PageIterator,
  Paginator,
} from "../sdk/types/operations.js";

/**
 * This is a paginated operation where there is both a query parameter and
 * request body field called "cursor". This ambiguity is used to test that
 * the generator only updates the appropriate field in the pagination code
 * "next()" function.
 */
export async function paginationPaginationAmbiguousInput(
  client: SDKCore,
  requestBody: operations.PaginationAmbiguousInputRequestBody,
  cursor?: number | undefined,
  options?: RequestOptions,
): Promise<
  PageIterator<
    Result<
      operations.PaginationAmbiguousInputResponse,
      | SDKError
      | SDKValidationError
      | UnexpectedClientError
      | InvalidRequestError
      | RequestAbortedError
      | RequestTimeoutError
      | ConnectionError
    >,
    { cursor: number }
  >
> {
  const input: operations.PaginationAmbiguousInputRequest = {
    requestBody: requestBody,
    cursor: cursor,
  };

  const parsed = safeParse(
    input,
    (value) =>
      operations.PaginationAmbiguousInputRequest$outboundSchema.parse(value),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return haltIterator(parsed);
  }
  const payload = parsed.value;
  const body = encodeJSON("body", payload.RequestBody, { explode: true });

  const baseURL = options?.serverURL
    || pathToFunc(PaginationAmbiguousInputServerList[0], {
      charEncoding: "percent",
    })();

  const path = pathToFunc("/pagination/cursor#ambiguousInput")();

  const query = encodeFormQuery({
    "cursor": payload.cursor,
  });

  const headers = new Headers(compactMap({
    "Content-Type": "application/json",
    Accept: "application/json",
  }));

  const securityInput = await extractSecurity(client._options.security);
  const requestSecurity = resolveGlobalSecurity(securityInput);

  const context = {
    operationID: "paginationAmbiguousInput",
    oAuth2Scopes: [],

    resolvedSecurity: requestSecurity,

    securitySource: client._options.security,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "PUT",
    baseURL: baseURL,
    path: path,
    headers: headers,
    query: query,
    body: body,
    uaHeader: "x-speakeasy-user-agent",
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return haltIterator(requestRes);
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    errorCodes: ["4XX", "5XX"],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return haltIterator(doResult);
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result, raw] = await M.match<
    operations.PaginationAmbiguousInputResponse,
    | SDKError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >(
    M.json(200, operations.PaginationAmbiguousInputResponse$inboundSchema, {
      key: "Result",
    }),
    M.fail(["4XX", "5XX"]),
  )(response, { extraFields: responseFields });
  if (!result.ok) {
    return haltIterator(result);
  }

  const nextFunc = (
    responseData: unknown,
  ): {
    next: Paginator<
      Result<
        operations.PaginationAmbiguousInputResponse,
        | SDKError
        | SDKValidationError
        | UnexpectedClientError
        | InvalidRequestError
        | RequestAbortedError
        | RequestTimeoutError
        | ConnectionError
      >
    >;
    "~next"?: { cursor: number };
  } => {
    const nextCursor = jp.value(responseData, "$.resultArray[(@.length-1)]");
    if (nextCursor == null) {
      return { next: () => null };
    }

    const nextVal = () =>
      paginationPaginationAmbiguousInput(
        client,
        {
          ...input,
          cursor: nextCursor,
        },
        cursor,
        options,
      );

    return { next: nextVal, "~next": { cursor: nextCursor } };
  };

  const page = { ...result, ...nextFunc(raw) };
  return { ...page, ...createPageIterator(page, (v) => !v.ok) };
}
