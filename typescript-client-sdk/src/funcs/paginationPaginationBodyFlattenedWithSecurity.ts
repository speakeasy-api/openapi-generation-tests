/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { SDKCore } from "../core.js";
import { dlv } from "../lib/dlv.js";
import { encodeFormQuery } from "../lib/encodings.js";
import * as M from "../lib/matchers.js";
import { safeParse } from "../lib/schemas.js";
import { RequestOptions } from "../lib/sdks.js";
import { resolveSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import {
  ConnectionError,
  InvalidRequestError,
  RequestAbortedError,
  RequestTimeoutError,
  UnexpectedClientError,
} from "../sdk/models/errors/httpclienterrors.js";
import { SDKError } from "../sdk/models/errors/sdkerror.js";
import { SDKValidationError } from "../sdk/models/errors/sdkvalidationerror.js";
import * as operations from "../sdk/models/operations/index.js";
import { PaginationBodyFlattenedWithSecurityServerList } from "../sdk/models/operations/paginationbodyflattenedwithsecurity.js";
import { Result } from "../sdk/types/fp.js";
import {
  createPageIterator,
  haltIterator,
  PageIterator,
  Paginator,
} from "../sdk/types/operations.js";

export async function paginationPaginationBodyFlattenedWithSecurity(
  client: SDKCore,
  security: operations.PaginationBodyFlattenedWithSecuritySecurity,
  limit: number,
  offset: number,
  options?: RequestOptions & { serverURL?: string },
): Promise<
  PageIterator<
    Result<
      operations.PaginationBodyFlattenedWithSecurityResponse,
      | SDKError
      | SDKValidationError
      | UnexpectedClientError
      | InvalidRequestError
      | RequestAbortedError
      | RequestTimeoutError
      | ConnectionError
    >
  >
> {
  const input: operations.PaginationBodyFlattenedWithSecurityRequest = {
    limit: limit,
    offset: offset,
  };

  const parsed = safeParse(
    input,
    (value) =>
      operations.PaginationBodyFlattenedWithSecurityRequest$outboundSchema
        .parse(value),
    "Input validation failed",
  );
  if (!parsed.ok) {
    return haltIterator(parsed);
  }
  const payload = parsed.value;
  const body = null;

  const baseURL = options?.serverURL
    || pathToFunc(PaginationBodyFlattenedWithSecurityServerList[0], {
      charEncoding: "percent",
    })();

  const path = pathToFunc(
    "/pagination/limitoffset/offset#paginationBodyFlattenedWithSecurity",
  )();

  const query = encodeFormQuery({
    "limit": payload.limit,
    "offset": payload.offset,
  });

  const headers = new Headers({
    Accept: "application/json",
  });

  const requestSecurity = resolveSecurity(
    [
      {
        fieldName: "x-api-key",
        type: "apiKey:header",
        value: security?.paginationAuth,
      },
    ],
  );

  const context = {
    operationID: "paginationBodyFlattenedWithSecurity",
    oAuth2Scopes: [],
    securitySource: security,
    retryConfig: options?.retries
      || client._options.retryConfig
      || { strategy: "none" },
    retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
  };

  const requestRes = client._createRequest(context, {
    security: requestSecurity,
    method: "GET",
    baseURL: baseURL,
    path: path,
    headers: headers,
    query: query,
    body: body,
    uaHeader: "x-speakeasy-user-agent",
    timeoutMs: options?.timeoutMs || client._options.timeoutMs || -1,
  }, options);
  if (!requestRes.ok) {
    return haltIterator(requestRes);
  }
  const req = requestRes.value;

  const doResult = await client._do(req, {
    context,
    errorCodes: ["4XX", "5XX"],
    retryConfig: context.retryConfig,
    retryCodes: context.retryCodes,
  });
  if (!doResult.ok) {
    return haltIterator(doResult);
  }
  const response = doResult.value;

  const responseFields = {
    HttpMeta: { Response: response, Request: req },
  };

  const [result, raw] = await M.match<
    operations.PaginationBodyFlattenedWithSecurityResponse,
    | SDKError
    | SDKValidationError
    | UnexpectedClientError
    | InvalidRequestError
    | RequestAbortedError
    | RequestTimeoutError
    | ConnectionError
  >(
    M.json(
      200,
      operations.PaginationBodyFlattenedWithSecurityResponse$inboundSchema,
      { key: "Result" },
    ),
    M.fail(["4XX", "5XX"]),
  )(response, { extraFields: responseFields });
  if (!result.ok) {
    return haltIterator(result);
  }

  const nextFunc = (
    responseData: unknown,
  ): Paginator<
    Result<
      operations.PaginationBodyFlattenedWithSecurityResponse,
      | SDKError
      | SDKValidationError
      | UnexpectedClientError
      | InvalidRequestError
      | RequestAbortedError
      | RequestTimeoutError
      | ConnectionError
    >
  > => {
    const offset = input?.offset || 0;

    if (!responseData) {
      return () => null;
    }
    const results = dlv(responseData, "resultArray");
    if (!Array.isArray(results) || !results.length) {
      return () => null;
    }
    const limit = input?.limit || 0;
    if (results.length < limit) {
      return () => null;
    }
    const nextOffset = offset + results.length;

    return () =>
      paginationPaginationBodyFlattenedWithSecurity(
        client,
        security,
        limit,
        nextOffset,
        options,
      );
  };

  const page = { ...result, next: nextFunc(raw) };
  return { ...page, ...createPageIterator(page, (v) => !v.ok) };
}
