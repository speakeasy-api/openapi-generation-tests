/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { SDKCore } from "../core.js";
import { SDKValidationError } from "../sdk/models/errors/sdkvalidationerror.js";
import * as shared from "../sdk/models/shared/index.js";
import { ERR, Result } from "../sdk/types/fp.js";
import { WebhookAuthenticationError } from "../sdk/types/webhooks.js";

export async function validateWebhook(client: SDKCore, {
  request: rawRequest,
  secret,
}: {
  request: {
    body: BodyInit;
    method: string;
    url: string;
    headers: Record<string, string> | Headers;
  } | Request;
  secret: string;
}): Promise<
  Result<
    shared.WebhookRequestCreated | shared.WebhookRequestUpdated,
    SDKValidationError | WebhookAuthenticationError
  >
> {
  const request = normalizeRequest(rawRequest);
  const verifyResult = await client._verifyWebhook({ request, secret });
  if (!verifyResult.ok) {
    return verifyResult;
  }
  const knownSchemas = [
    shared.webhookRequestCreatedFromJSON,
    shared.webhookRequestUpdatedFromJSON,
  ];

  const jsonString = await request.text();

  for (const schema of knownSchemas) {
    const ret = schema(jsonString);
    if (ret.ok) {
      return ret;
    }
  }

  return ERR(
    new SDKValidationError(
      "No matching schema found for the given webhook payload",
      jsonString,
      jsonString,
    ),
  );
}

function normalizeRequest(
  request: {
    body: BodyInit;
    method: string;
    url: string;
    headers: Record<string, string> | Headers;
  } | Request,
): Request {
  if (request instanceof Request) {
    return request;
  }
  return new Request(request.url, request);
}
