/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { SDKHooks } from "../hooks";
import { SDK_METADATA, SDKOptions, serverURLFromOptions } from "../lib/config";
import * as enc$ from "../lib/encodings";
import { HTTPClient } from "../lib/http";
import * as schemas$ from "../lib/schemas";
import { ClientSDK, RequestOptions } from "../lib/sdks";
import { SecurityInput } from "../lib/security";
import * as operations from "./models/operations";
import * as shared from "./models/shared";

export class AuthNew extends ClientSDK {
    private readonly options$: SDKOptions & { hooks?: SDKHooks };

    constructor(options: SDKOptions = {}) {
        const opt = options as unknown;
        let hooks: SDKHooks;
        if (
            typeof opt === "object" &&
            opt != null &&
            "hooks" in opt &&
            opt.hooks instanceof SDKHooks
        ) {
            hooks = opt.hooks;
        } else {
            hooks = new SDKHooks();
        }

        super({
            client: options.httpClient || new HTTPClient(),
            baseURL: serverURLFromOptions(options),
            hooks,
        });

        this.options$ = { ...options, hooks };
        void this.options$;
    }

    async authGlobal(
        request: shared.AuthServiceRequestBody,
        options?: RequestOptions & { serverURL?: string }
    ): Promise<operations.AuthGlobalResponse> {
        const input$ = request;
        const headers$ = new Headers();
        headers$.set("x-speakeasy-user-agent", SDK_METADATA.userAgent);
        headers$.set("Content-Type", "application/json");
        headers$.set("Accept", "*/*");

        const payload$ = schemas$.parse(
            input$,
            (value$) => shared.AuthServiceRequestBody$.outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = enc$.encodeJSON("body", payload$, { explode: true });
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(operations.AuthGlobalServerList[0], {
                charEncoding: "percent",
            })();

        const path$ = this.templateURLComponent("/auth#authGlobal")();

        const query$ = "";

        const security$ =
            typeof this.options$.security === "function"
                ? await this.options$.security()
                : this.options$.security;

        const context = {
            operationID: "authGlobal",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const doOptions = { context, errorCodes: ["401", "4XX", "5XX"] };
        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
            },
            options
        );

        const response = await this.do$(request$, doOptions);

        const responseFields$ = {
            ContentType: response.headers.get("content-type") ?? "application/octet-stream",
            StatusCode: response.status,
            RawResponse: response,
            Headers: {},
        };

        const [result$] = await this.matcher<operations.AuthGlobalResponse>()
            .void(200, operations.AuthGlobalResponse$)
            .fail([401, "4XX", "5XX"])
            .match(response, { extraFields: responseFields$ });

        return result$;
    }

    async basicAuthNew(
        request: shared.AuthServiceRequestBody,
        security: operations.BasicAuthNewSecurity,
        options?: RequestOptions & { serverURL?: string }
    ): Promise<operations.BasicAuthNewResponse> {
        const input$ = request;
        const headers$ = new Headers();
        headers$.set("x-speakeasy-user-agent", SDK_METADATA.userAgent);
        headers$.set("Content-Type", "application/json");
        headers$.set("Accept", "*/*");

        const payload$ = schemas$.parse(
            input$,
            (value$) => shared.AuthServiceRequestBody$.outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = enc$.encodeJSON("body", payload$, { explode: true });
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(operations.BasicAuthNewServerList[0], {
                charEncoding: "percent",
            })();

        const path$ = this.templateURLComponent("/auth#basicAuth")();

        const query$ = "";

        const security$: SecurityInput[][] = [
            [
                {
                    type: "http:basic",
                    value: { username: security?.username, password: security?.password },
                },
            ],
        ];
        const securitySettings$ = this.resolveSecurity(...security$);
        const context = {
            operationID: "basicAuthNew",
            oAuth2Scopes: [],
            securitySource: security$,
        };

        const doOptions = { context, errorCodes: ["401", "4XX", "5XX"] };
        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
            },
            options
        );

        const response = await this.do$(request$, doOptions);

        const responseFields$ = {
            ContentType: response.headers.get("content-type") ?? "application/octet-stream",
            StatusCode: response.status,
            RawResponse: response,
            Headers: {},
        };

        const [result$] = await this.matcher<operations.BasicAuthNewResponse>()
            .void(200, operations.BasicAuthNewResponse$)
            .fail([401, "4XX", "5XX"])
            .match(response, { extraFields: responseFields$ });

        return result$;
    }

    async multipleMixedOptionsAuth(
        request: shared.AuthServiceRequestBody,
        security: operations.MultipleMixedOptionsAuthSecurity,
        options?: RequestOptions & { serverURL?: string }
    ): Promise<operations.MultipleMixedOptionsAuthResponse> {
        const input$ = request;
        const headers$ = new Headers();
        headers$.set("x-speakeasy-user-agent", SDK_METADATA.userAgent);
        headers$.set("Content-Type", "application/json");
        headers$.set("Accept", "*/*");

        const payload$ = schemas$.parse(
            input$,
            (value$) => shared.AuthServiceRequestBody$.outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = enc$.encodeJSON("body", payload$, { explode: true });
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(operations.MultipleMixedOptionsAuthServerList[0], {
                charEncoding: "percent",
            })();

        const path$ = this.templateURLComponent("/auth#multipleMixedOptionsAuth")();

        const query$ = "";

        const security$: SecurityInput[][] = [
            [
                {
                    fieldName: "x-api-key",
                    type: "apiKey:header",
                    value: security?.apiKeyAuthNew,
                },
            ],
            [
                {
                    type: "http:basic",
                    value: {
                        username: security?.basicAuth?.username,
                        password: security?.basicAuth?.password,
                    },
                },
            ],
        ];
        const securitySettings$ = this.resolveSecurity(...security$);
        const context = {
            operationID: "multipleMixedOptionsAuth",
            oAuth2Scopes: [],
            securitySource: security$,
        };

        const doOptions = { context, errorCodes: ["401", "4XX", "5XX"] };
        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
            },
            options
        );

        const response = await this.do$(request$, doOptions);

        const responseFields$ = {
            ContentType: response.headers.get("content-type") ?? "application/octet-stream",
            StatusCode: response.status,
            RawResponse: response,
            Headers: {},
        };

        const [result$] = await this.matcher<operations.MultipleMixedOptionsAuthResponse>()
            .void(200, operations.MultipleMixedOptionsAuthResponse$)
            .fail([401, "4XX", "5XX"])
            .match(response, { extraFields: responseFields$ });

        return result$;
    }

    async multipleMixedSchemeAuth(
        request: shared.AuthServiceRequestBody,
        security: operations.MultipleMixedSchemeAuthSecurity,
        options?: RequestOptions & { serverURL?: string }
    ): Promise<operations.MultipleMixedSchemeAuthResponse> {
        const input$ = request;
        const headers$ = new Headers();
        headers$.set("x-speakeasy-user-agent", SDK_METADATA.userAgent);
        headers$.set("Content-Type", "application/json");
        headers$.set("Accept", "*/*");

        const payload$ = schemas$.parse(
            input$,
            (value$) => shared.AuthServiceRequestBody$.outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = enc$.encodeJSON("body", payload$, { explode: true });
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(operations.MultipleMixedSchemeAuthServerList[0], {
                charEncoding: "percent",
            })();

        const path$ = this.templateURLComponent("/auth#multipleMixedSchemeAuth")();

        const query$ = "";

        const security$: SecurityInput[][] = [
            [
                {
                    fieldName: "x-api-key",
                    type: "apiKey:header",
                    value: security?.apiKeyAuthNew,
                },
                {
                    type: "http:basic",
                    value: {
                        username: security?.basicAuth?.username,
                        password: security?.basicAuth?.password,
                    },
                },
            ],
        ];
        const securitySettings$ = this.resolveSecurity(...security$);
        const context = {
            operationID: "multipleMixedSchemeAuth",
            oAuth2Scopes: [],
            securitySource: security$,
        };

        const doOptions = { context, errorCodes: ["401", "4XX", "5XX"] };
        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
            },
            options
        );

        const response = await this.do$(request$, doOptions);

        const responseFields$ = {
            ContentType: response.headers.get("content-type") ?? "application/octet-stream",
            StatusCode: response.status,
            RawResponse: response,
            Headers: {},
        };

        const [result$] = await this.matcher<operations.MultipleMixedSchemeAuthResponse>()
            .void(200, operations.MultipleMixedSchemeAuthResponse$)
            .fail([401, "4XX", "5XX"])
            .match(response, { extraFields: responseFields$ });

        return result$;
    }

    async multipleOptionsWithMixedSchemesAuth(
        request: shared.AuthServiceRequestBody,
        security: operations.MultipleOptionsWithMixedSchemesAuthSecurity,
        options?: RequestOptions & { serverURL?: string }
    ): Promise<operations.MultipleOptionsWithMixedSchemesAuthResponse> {
        const input$ = request;
        const headers$ = new Headers();
        headers$.set("x-speakeasy-user-agent", SDK_METADATA.userAgent);
        headers$.set("Content-Type", "application/json");
        headers$.set("Accept", "*/*");

        const payload$ = schemas$.parse(
            input$,
            (value$) => shared.AuthServiceRequestBody$.outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = enc$.encodeJSON("body", payload$, { explode: true });
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(operations.MultipleOptionsWithMixedSchemesAuthServerList[0], {
                charEncoding: "percent",
            })();

        const path$ = this.templateURLComponent("/auth#multipleOptionsWithMixedSchemesAuth")();

        const query$ = "";

        const security$: SecurityInput[][] = [
            [
                {
                    fieldName: "x-api-key",
                    type: "apiKey:header",
                    value: security?.option1?.apiKeyAuthNew,
                },
                {
                    fieldName: "Authorization",
                    type: "oauth2",
                    value: security?.option1?.oauth2,
                },
            ],
            [
                {
                    fieldName: "x-api-key",
                    type: "apiKey:header",
                    value: security?.option2?.apiKeyAuthNew,
                },
                {
                    type: "http:basic",
                    value: {
                        username: security?.option2?.basicAuth?.username,
                        password: security?.option2?.basicAuth?.password,
                    },
                },
            ],
        ];
        const securitySettings$ = this.resolveSecurity(...security$);
        const context = {
            operationID: "multipleOptionsWithMixedSchemesAuth",
            oAuth2Scopes: [],
            securitySource: security$,
        };

        const doOptions = { context, errorCodes: ["401", "4XX", "5XX"] };
        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
            },
            options
        );

        const response = await this.do$(request$, doOptions);

        const responseFields$ = {
            ContentType: response.headers.get("content-type") ?? "application/octet-stream",
            StatusCode: response.status,
            RawResponse: response,
            Headers: {},
        };

        const [result$] =
            await this.matcher<operations.MultipleOptionsWithMixedSchemesAuthResponse>()
                .void(200, operations.MultipleOptionsWithMixedSchemesAuthResponse$)
                .fail([401, "4XX", "5XX"])
                .match(response, { extraFields: responseFields$ });

        return result$;
    }

    async multipleOptionsWithSimpleSchemesAuth(
        request: shared.AuthServiceRequestBody,
        security: operations.MultipleOptionsWithSimpleSchemesAuthSecurity,
        options?: RequestOptions & { serverURL?: string }
    ): Promise<operations.MultipleOptionsWithSimpleSchemesAuthResponse> {
        const input$ = request;
        const headers$ = new Headers();
        headers$.set("x-speakeasy-user-agent", SDK_METADATA.userAgent);
        headers$.set("Content-Type", "application/json");
        headers$.set("Accept", "*/*");

        const payload$ = schemas$.parse(
            input$,
            (value$) => shared.AuthServiceRequestBody$.outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = enc$.encodeJSON("body", payload$, { explode: true });
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(
                operations.MultipleOptionsWithSimpleSchemesAuthServerList[0],
                { charEncoding: "percent" }
            )();

        const path$ = this.templateURLComponent("/auth#multipleOptionsWithSimpleSchemesAuth")();

        const query$ = "";

        const security$: SecurityInput[][] = [
            [
                {
                    fieldName: "x-api-key",
                    type: "apiKey:header",
                    value: security?.option1?.apiKeyAuthNew,
                },
                {
                    fieldName: "Authorization",
                    type: "oauth2",
                    value: security?.option1?.oauth2,
                },
            ],
            [
                {
                    fieldName: "x-api-key",
                    type: "apiKey:header",
                    value: security?.option2?.apiKeyAuthNew,
                },
                {
                    fieldName: "Authorization",
                    type: "openIdConnect",
                    value: security?.option2?.openIdConnect,
                },
            ],
        ];
        const securitySettings$ = this.resolveSecurity(...security$);
        const context = {
            operationID: "multipleOptionsWithSimpleSchemesAuth",
            oAuth2Scopes: [],
            securitySource: security$,
        };

        const doOptions = { context, errorCodes: ["401", "4XX", "5XX"] };
        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
            },
            options
        );

        const response = await this.do$(request$, doOptions);

        const responseFields$ = {
            ContentType: response.headers.get("content-type") ?? "application/octet-stream",
            StatusCode: response.status,
            RawResponse: response,
            Headers: {},
        };

        const [result$] =
            await this.matcher<operations.MultipleOptionsWithSimpleSchemesAuthResponse>()
                .void(200, operations.MultipleOptionsWithSimpleSchemesAuthResponse$)
                .fail([401, "4XX", "5XX"])
                .match(response, { extraFields: responseFields$ });

        return result$;
    }

    async multipleSimpleOptionsAuth(
        request: shared.AuthServiceRequestBody,
        security: operations.MultipleSimpleOptionsAuthSecurity,
        options?: RequestOptions & { serverURL?: string }
    ): Promise<operations.MultipleSimpleOptionsAuthResponse> {
        const input$ = request;
        const headers$ = new Headers();
        headers$.set("x-speakeasy-user-agent", SDK_METADATA.userAgent);
        headers$.set("Content-Type", "application/json");
        headers$.set("Accept", "*/*");

        const payload$ = schemas$.parse(
            input$,
            (value$) => shared.AuthServiceRequestBody$.outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = enc$.encodeJSON("body", payload$, { explode: true });
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(operations.MultipleSimpleOptionsAuthServerList[0], {
                charEncoding: "percent",
            })();

        const path$ = this.templateURLComponent("/auth#multipleSimpleOptionsAuth")();

        const query$ = "";

        const security$: SecurityInput[][] = [
            [
                {
                    fieldName: "x-api-key",
                    type: "apiKey:header",
                    value: security?.apiKeyAuthNew,
                },
            ],
            [
                {
                    fieldName: "Authorization",
                    type: "oauth2",
                    value: security?.oauth2,
                },
            ],
        ];
        const securitySettings$ = this.resolveSecurity(...security$);
        const context = {
            operationID: "multipleSimpleOptionsAuth",
            oAuth2Scopes: [],
            securitySource: security$,
        };

        const doOptions = { context, errorCodes: ["401", "4XX", "5XX"] };
        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
            },
            options
        );

        const response = await this.do$(request$, doOptions);

        const responseFields$ = {
            ContentType: response.headers.get("content-type") ?? "application/octet-stream",
            StatusCode: response.status,
            RawResponse: response,
            Headers: {},
        };

        const [result$] = await this.matcher<operations.MultipleSimpleOptionsAuthResponse>()
            .void(200, operations.MultipleSimpleOptionsAuthResponse$)
            .fail([401, "4XX", "5XX"])
            .match(response, { extraFields: responseFields$ });

        return result$;
    }

    async multipleSimpleSchemeAuth(
        request: shared.AuthServiceRequestBody,
        security: operations.MultipleSimpleSchemeAuthSecurity,
        options?: RequestOptions & { serverURL?: string }
    ): Promise<operations.MultipleSimpleSchemeAuthResponse> {
        const input$ = request;
        const headers$ = new Headers();
        headers$.set("x-speakeasy-user-agent", SDK_METADATA.userAgent);
        headers$.set("Content-Type", "application/json");
        headers$.set("Accept", "*/*");

        const payload$ = schemas$.parse(
            input$,
            (value$) => shared.AuthServiceRequestBody$.outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = enc$.encodeJSON("body", payload$, { explode: true });
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(operations.MultipleSimpleSchemeAuthServerList[0], {
                charEncoding: "percent",
            })();

        const path$ = this.templateURLComponent("/auth#multipleSimpleSchemeAuth")();

        const query$ = "";

        const security$: SecurityInput[][] = [
            [
                {
                    fieldName: "x-api-key",
                    type: "apiKey:header",
                    value: security?.apiKeyAuthNew,
                },
                {
                    fieldName: "Authorization",
                    type: "oauth2",
                    value: security?.oauth2,
                },
            ],
        ];
        const securitySettings$ = this.resolveSecurity(...security$);
        const context = {
            operationID: "multipleSimpleSchemeAuth",
            oAuth2Scopes: [],
            securitySource: security$,
        };

        const doOptions = { context, errorCodes: ["401", "4XX", "5XX"] };
        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
            },
            options
        );

        const response = await this.do$(request$, doOptions);

        const responseFields$ = {
            ContentType: response.headers.get("content-type") ?? "application/octet-stream",
            StatusCode: response.status,
            RawResponse: response,
            Headers: {},
        };

        const [result$] = await this.matcher<operations.MultipleSimpleSchemeAuthResponse>()
            .void(200, operations.MultipleSimpleSchemeAuthResponse$)
            .fail([401, "4XX", "5XX"])
            .match(response, { extraFields: responseFields$ });

        return result$;
    }

    async oauth2AuthNew(
        request: shared.AuthServiceRequestBody,
        options?: RequestOptions & { serverURL?: string }
    ): Promise<operations.Oauth2AuthNewResponse> {
        const input$ = request;
        const headers$ = new Headers();
        headers$.set("x-speakeasy-user-agent", SDK_METADATA.userAgent);
        headers$.set("Content-Type", "application/json");
        headers$.set("Accept", "*/*");

        const payload$ = schemas$.parse(
            input$,
            (value$) => shared.AuthServiceRequestBody$.outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = enc$.encodeJSON("body", payload$, { explode: true });
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(operations.Oauth2AuthNewServerList[0], {
                charEncoding: "percent",
            })();

        const path$ = this.templateURLComponent("/auth#oauth2Auth")();

        const query$ = "";

        const security$ =
            typeof this.options$.security === "function"
                ? await this.options$.security()
                : this.options$.security;

        const context = {
            operationID: "oauth2AuthNew",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const doOptions = { context, errorCodes: ["401", "4XX", "5XX"] };
        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
            },
            options
        );

        const response = await this.do$(request$, doOptions);

        const responseFields$ = {
            ContentType: response.headers.get("content-type") ?? "application/octet-stream",
            StatusCode: response.status,
            RawResponse: response,
            Headers: {},
        };

        const [result$] = await this.matcher<operations.Oauth2AuthNewResponse>()
            .void(200, operations.Oauth2AuthNewResponse$)
            .fail([401, "4XX", "5XX"])
            .match(response, { extraFields: responseFields$ });

        return result$;
    }

    async openIdConnectAuthNew(
        request: shared.AuthServiceRequestBody,
        security: operations.OpenIdConnectAuthNewSecurity,
        options?: RequestOptions & { serverURL?: string }
    ): Promise<operations.OpenIdConnectAuthNewResponse> {
        const input$ = request;
        const headers$ = new Headers();
        headers$.set("x-speakeasy-user-agent", SDK_METADATA.userAgent);
        headers$.set("Content-Type", "application/json");
        headers$.set("Accept", "*/*");

        const payload$ = schemas$.parse(
            input$,
            (value$) => shared.AuthServiceRequestBody$.outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = enc$.encodeJSON("body", payload$, { explode: true });
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(operations.OpenIdConnectAuthNewServerList[0], {
                charEncoding: "percent",
            })();

        const path$ = this.templateURLComponent("/auth#openIdConnectAuth")();

        const query$ = "";

        const security$: SecurityInput[][] = [
            [
                {
                    fieldName: "Authorization",
                    type: "openIdConnect",
                    value: security?.openIdConnect,
                },
            ],
        ];
        const securitySettings$ = this.resolveSecurity(...security$);
        const context = {
            operationID: "openIdConnectAuthNew",
            oAuth2Scopes: [],
            securitySource: security$,
        };

        const doOptions = { context, errorCodes: ["401", "4XX", "5XX"] };
        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
            },
            options
        );

        const response = await this.do$(request$, doOptions);

        const responseFields$ = {
            ContentType: response.headers.get("content-type") ?? "application/octet-stream",
            StatusCode: response.status,
            RawResponse: response,
            Headers: {},
        };

        const [result$] = await this.matcher<operations.OpenIdConnectAuthNewResponse>()
            .void(200, operations.OpenIdConnectAuthNewResponse$)
            .fail([401, "4XX", "5XX"])
            .match(response, { extraFields: responseFields$ });

        return result$;
    }
}
