/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { SDKHooks } from "../hooks/hooks.js";
import { SDKOptions, serverURLFromOptions } from "../lib/config.js";
import { encodeJSON as encodeJSON$ } from "../lib/encodings.js";
import { EventStream } from "../lib/event-streams.js";
import { HTTPClient } from "../lib/http.js";
import * as schemas$ from "../lib/schemas.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import { ChatServerList } from "./models/operations/chat.js";
import { ChatSkipSentinelServerList } from "./models/operations/chatskipsentinel.js";
import { DifferentDataSchemasServerList } from "./models/operations/differentdataschemas.js";
import * as operations from "./models/operations/index.js";
import { JsonServerList } from "./models/operations/json.js";
import { MultilineServerList } from "./models/operations/multiline.js";
import { RichServerList } from "./models/operations/rich.js";
import { TextServerList } from "./models/operations/text.js";
import * as shared from "./models/shared/index.js";
import * as z from "zod";

export enum ChatAcceptEnum {
    applicationJson = "application/json",
    textEventStream = "text/event-stream",
}

export class Eventstreams extends ClientSDK {
    private readonly options$: SDKOptions & { hooks?: SDKHooks };

    constructor(options: SDKOptions = {}) {
        const opt = options as unknown;
        let hooks: SDKHooks;
        if (
            typeof opt === "object" &&
            opt != null &&
            "hooks" in opt &&
            opt.hooks instanceof SDKHooks
        ) {
            hooks = opt.hooks;
        } else {
            hooks = new SDKHooks();
        }

        super({
            client: options.httpClient || new HTTPClient(),
            baseURL: serverURLFromOptions(options),
            hooks,
        });

        this.options$ = { ...options, hooks };
        void this.options$;
    }

    async chat(
        request: operations.ChatRequestBody,
        options?: RequestOptions & { acceptHeaderOverride?: ChatAcceptEnum; serverURL?: string }
    ): Promise<operations.ChatResponse> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ChatRequestBody$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$, { explode: true });
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(ChatServerList[0], { charEncoding: "percent" })();

        const path$ = this.templateURLComponent("/eventstreams/chat")();

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: options?.acceptHeaderOverride || "application/json;q=1, text/event-stream;q=0",
        });

        const security$ =
            typeof this.options$.security === "function"
                ? await this.options$.security()
                : this.options$.security;

        const context = {
            operationID: "chat",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<operations.ChatResponse>()
            .json(200, operations.ChatResponse$inboundSchema)
            .sse(200, operations.ChatResponse$inboundSchema)
            .fail(["4XX", "5XX"])
            .match(response);

        return result$;
    }

    async chatSkipSentinel(
        request: operations.ChatSkipSentinelRequestBody,
        options?: RequestOptions & { serverURL?: string }
    ): Promise<EventStream<shared.ChatCompletionEvent>> {
        const input$ = request;

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.ChatSkipSentinelRequestBody$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$, { explode: true });
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(ChatSkipSentinelServerList[0], { charEncoding: "percent" })();

        const path$ = this.templateURLComponent("/eventstreams/chat-chunked")();

        const query$ = "";

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "text/event-stream",
        });

        const security$ =
            typeof this.options$.security === "function"
                ? await this.options$.security()
                : this.options$.security;

        const context = {
            operationID: "chatSkipSentinel",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<EventStream<shared.ChatCompletionEvent>>()
            .sse(
                200,
                z.instanceof(ReadableStream<Uint8Array>).transform((stream) => {
                    return new EventStream({
                        stream,
                        decoder(rawEvent) {
                            const schema = shared.ChatCompletionEvent$inboundSchema;
                            return schema.parse(rawEvent);
                        },
                    });
                }),
                { sseSentinel: "[DONE]" }
            )
            .fail(["4XX", "5XX"])
            .match(response);

        return result$;
    }

    async differentDataSchemas(
        options?: RequestOptions & { serverURL?: string }
    ): Promise<EventStream<shared.DifferentDataSchemas>> {
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(DifferentDataSchemasServerList[0], {
                charEncoding: "percent",
            })();

        const path$ = this.templateURLComponent("/eventstreams/differentdataschemas")();

        const query$ = "";

        const headers$ = new Headers({
            Accept: "text/event-stream",
        });

        const security$ =
            typeof this.options$.security === "function"
                ? await this.options$.security()
                : this.options$.security;

        const context = {
            operationID: "differentDataSchemas",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<EventStream<shared.DifferentDataSchemas>>()
            .sse(
                200,
                z.instanceof(ReadableStream<Uint8Array>).transform((stream) => {
                    return new EventStream({
                        stream,
                        decoder(rawEvent) {
                            const schema = shared.DifferentDataSchemas$inboundSchema;
                            return schema.parse(rawEvent);
                        },
                    });
                })
            )
            .fail(["4XX", "5XX"])
            .match(response);

        return result$;
    }

    async json(
        options?: RequestOptions & { serverURL?: string }
    ): Promise<operations.JsonResponse> {
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(JsonServerList[0], { charEncoding: "percent" })();

        const path$ = this.templateURLComponent("/eventstreams/json")();

        const query$ = "";

        const headers$ = new Headers({
            Accept: "text/event-stream",
        });

        const security$ =
            typeof this.options$.security === "function"
                ? await this.options$.security()
                : this.options$.security;

        const context = {
            operationID: "json",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const responseFields$ = {
            HttpMeta: { Response: response, Request: request$ },
        };

        const [result$] = await this.matcher<operations.JsonResponse>()
            .sse(200, operations.JsonResponse$inboundSchema, { hdrs: true, key: "Result" })
            .fail(["4XX", "5XX"])
            .match(response, { extraFields: responseFields$ });

        return result$;
    }

    async multiline(
        options?: RequestOptions & { serverURL?: string }
    ): Promise<EventStream<shared.TextEvent>> {
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(MultilineServerList[0], { charEncoding: "percent" })();

        const path$ = this.templateURLComponent("/eventstreams/multiline")();

        const query$ = "";

        const headers$ = new Headers({
            Accept: "text/event-stream",
        });

        const security$ =
            typeof this.options$.security === "function"
                ? await this.options$.security()
                : this.options$.security;

        const context = {
            operationID: "multiline",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<EventStream<shared.TextEvent>>()
            .sse(
                200,
                z.instanceof(ReadableStream<Uint8Array>).transform((stream) => {
                    return new EventStream({
                        stream,
                        decoder(rawEvent) {
                            const schema = shared.TextEvent$inboundSchema;
                            return schema.parse(rawEvent);
                        },
                    });
                })
            )
            .fail(["4XX", "5XX"])
            .match(response);

        return result$;
    }

    async rich(
        options?: RequestOptions & { serverURL?: string }
    ): Promise<EventStream<shared.RichStream>> {
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(RichServerList[0], { charEncoding: "percent" })();

        const path$ = this.templateURLComponent("/eventstreams/rich")();

        const query$ = "";

        const headers$ = new Headers({
            Accept: "text/event-stream",
        });

        const security$ =
            typeof this.options$.security === "function"
                ? await this.options$.security()
                : this.options$.security;

        const context = {
            operationID: "rich",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<EventStream<shared.RichStream>>()
            .sse(
                200,
                z.instanceof(ReadableStream<Uint8Array>).transform((stream) => {
                    return new EventStream({
                        stream,
                        decoder(rawEvent) {
                            const schema = shared.RichStream$inboundSchema;
                            return schema.parse(rawEvent);
                        },
                    });
                })
            )
            .fail(["4XX", "5XX"])
            .match(response);

        return result$;
    }

    async text(
        options?: RequestOptions & { serverURL?: string }
    ): Promise<EventStream<shared.TextEvent>> {
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(TextServerList[0], { charEncoding: "percent" })();

        const path$ = this.templateURLComponent("/eventstreams/text")();

        const query$ = "";

        const headers$ = new Headers({
            Accept: "text/event-stream",
        });

        const security$ =
            typeof this.options$.security === "function"
                ? await this.options$.security()
                : this.options$.security;

        const context = {
            operationID: "text",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );

        const response = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });

        const [result$] = await this.matcher<EventStream<shared.TextEvent>>()
            .sse(
                200,
                z.instanceof(ReadableStream<Uint8Array>).transform((stream) => {
                    return new EventStream({
                        stream,
                        decoder(rawEvent) {
                            const schema = shared.TextEvent$inboundSchema;
                            return schema.parse(rawEvent);
                        },
                    });
                })
            )
            .fail(["4XX", "5XX"])
            .match(response);

        return result$;
    }
}
