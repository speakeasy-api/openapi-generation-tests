/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { encodeJSON as encodeJSON$ } from "../lib/encodings.js";
import { EventStream } from "../lib/event-streams.js";
import * as m$ from "../lib/matchers.js";
import * as schemas$ from "../lib/schemas.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { SDKError } from "./models/errors/sdkerror.js";
import { SDKValidationError } from "./models/errors/sdkvalidationerror.js";
import { ChatServerList } from "./models/operations/chat.js";
import { ChatSkipSentinelServerList } from "./models/operations/chatskipsentinel.js";
import { DifferentDataSchemasServerList } from "./models/operations/differentdataschemas.js";
import * as operations from "./models/operations/index.js";
import { JsonServerList } from "./models/operations/json.js";
import { MultilineServerList } from "./models/operations/multiline.js";
import { RichServerList } from "./models/operations/rich.js";
import { TextServerList } from "./models/operations/text.js";
import * as shared from "./models/shared/index.js";
import { unwrap as unwrap$ } from "./types/fp.js";
import * as z from "zod";

export enum ChatAcceptEnum {
    applicationJson = "application/json",
    textEventStream = "text/event-stream",
}

export class Eventstreams extends ClientSDK {
    async chat(
        request: operations.ChatRequestBody,
        options?: RequestOptions & { acceptHeaderOverride?: ChatAcceptEnum; serverURL?: string }
    ): Promise<operations.ChatResponse> {
        const input$ = request;

        const parsed$ = schemas$.safeParse(
            input$,
            (value$) => operations.ChatRequestBody$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const payload$ = unwrap$(parsed$);
        const body$ = encodeJSON$("body", payload$, { explode: true });

        const baseURL$ =
            options?.serverURL || pathToFunc(ChatServerList[0], { charEncoding: "percent" })();

        const path$ = pathToFunc("/eventstreams/chat")();

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: options?.acceptHeaderOverride || "application/json;q=1, text/event-stream;q=0",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "chat",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = resolveGlobalSecurity(security$);

        const requestRes$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                body: body$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );
        const request$ = unwrap$(requestRes$);

        const doResult = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });
        const response = unwrap$(doResult);

        const [result$] = await m$.match<operations.ChatResponse, SDKError | SDKValidationError>(
            m$.json(200, operations.ChatResponse$inboundSchema),
            m$.sse(200, operations.ChatResponse$inboundSchema),
            m$.fail(["4XX", "5XX"])
        )(response);

        return unwrap$(result$);
    }

    async chatSkipSentinel(
        request: operations.ChatSkipSentinelRequestBody,
        options?: RequestOptions & { serverURL?: string }
    ): Promise<EventStream<shared.ChatCompletionEvent>> {
        const input$ = request;

        const parsed$ = schemas$.safeParse(
            input$,
            (value$) => operations.ChatSkipSentinelRequestBody$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const payload$ = unwrap$(parsed$);
        const body$ = encodeJSON$("body", payload$, { explode: true });

        const baseURL$ =
            options?.serverURL ||
            pathToFunc(ChatSkipSentinelServerList[0], { charEncoding: "percent" })();

        const path$ = pathToFunc("/eventstreams/chat-chunked")();

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "text/event-stream",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "chatSkipSentinel",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = resolveGlobalSecurity(security$);

        const requestRes$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                body: body$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );
        const request$ = unwrap$(requestRes$);

        const doResult = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });
        const response = unwrap$(doResult);

        const [result$] = await m$.match<
            EventStream<shared.ChatCompletionEvent>,
            SDKError | SDKValidationError
        >(
            m$.sse(
                200,
                z.instanceof(ReadableStream<Uint8Array>).transform((stream) => {
                    return new EventStream({
                        stream,
                        decoder(rawEvent) {
                            const schema = shared.ChatCompletionEvent$inboundSchema;
                            return schema.parse(rawEvent);
                        },
                    });
                }),
                { sseSentinel: "[DONE]" }
            ),
            m$.fail(["4XX", "5XX"])
        )(response);

        return unwrap$(result$);
    }

    async differentDataSchemas(
        options?: RequestOptions & { serverURL?: string }
    ): Promise<EventStream<shared.DifferentDataSchemas>> {
        const baseURL$ =
            options?.serverURL ||
            pathToFunc(DifferentDataSchemasServerList[0], { charEncoding: "percent" })();

        const path$ = pathToFunc("/eventstreams/differentdataschemas")();

        const headers$ = new Headers({
            Accept: "text/event-stream",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "differentDataSchemas",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = resolveGlobalSecurity(security$);

        const requestRes$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );
        const request$ = unwrap$(requestRes$);

        const doResult = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });
        const response = unwrap$(doResult);

        const [result$] = await m$.match<
            EventStream<shared.DifferentDataSchemas>,
            SDKError | SDKValidationError
        >(
            m$.sse(
                200,
                z.instanceof(ReadableStream<Uint8Array>).transform((stream) => {
                    return new EventStream({
                        stream,
                        decoder(rawEvent) {
                            const schema = shared.DifferentDataSchemas$inboundSchema;
                            return schema.parse(rawEvent);
                        },
                    });
                })
            ),
            m$.fail(["4XX", "5XX"])
        )(response);

        return unwrap$(result$);
    }

    async json(
        options?: RequestOptions & { serverURL?: string }
    ): Promise<operations.JsonResponse> {
        const baseURL$ =
            options?.serverURL || pathToFunc(JsonServerList[0], { charEncoding: "percent" })();

        const path$ = pathToFunc("/eventstreams/json")();

        const headers$ = new Headers({
            Accept: "text/event-stream",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "json",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = resolveGlobalSecurity(security$);

        const requestRes$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );
        const request$ = unwrap$(requestRes$);

        const doResult = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });
        const response = unwrap$(doResult);

        const responseFields$ = {
            HttpMeta: { Response: response, Request: request$ },
        };

        const [result$] = await m$.match<operations.JsonResponse, SDKError | SDKValidationError>(
            m$.sse(200, operations.JsonResponse$inboundSchema, { hdrs: true, key: "Result" }),
            m$.fail(["4XX", "5XX"])
        )(response, { extraFields: responseFields$ });

        return unwrap$(result$);
    }

    async multiline(
        options?: RequestOptions & { serverURL?: string }
    ): Promise<EventStream<shared.TextEvent>> {
        const baseURL$ =
            options?.serverURL || pathToFunc(MultilineServerList[0], { charEncoding: "percent" })();

        const path$ = pathToFunc("/eventstreams/multiline")();

        const headers$ = new Headers({
            Accept: "text/event-stream",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "multiline",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = resolveGlobalSecurity(security$);

        const requestRes$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );
        const request$ = unwrap$(requestRes$);

        const doResult = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });
        const response = unwrap$(doResult);

        const [result$] = await m$.match<
            EventStream<shared.TextEvent>,
            SDKError | SDKValidationError
        >(
            m$.sse(
                200,
                z.instanceof(ReadableStream<Uint8Array>).transform((stream) => {
                    return new EventStream({
                        stream,
                        decoder(rawEvent) {
                            const schema = shared.TextEvent$inboundSchema;
                            return schema.parse(rawEvent);
                        },
                    });
                })
            ),
            m$.fail(["4XX", "5XX"])
        )(response);

        return unwrap$(result$);
    }

    async rich(
        options?: RequestOptions & { serverURL?: string }
    ): Promise<EventStream<shared.RichStream>> {
        const baseURL$ =
            options?.serverURL || pathToFunc(RichServerList[0], { charEncoding: "percent" })();

        const path$ = pathToFunc("/eventstreams/rich")();

        const headers$ = new Headers({
            Accept: "text/event-stream",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "rich",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = resolveGlobalSecurity(security$);

        const requestRes$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );
        const request$ = unwrap$(requestRes$);

        const doResult = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });
        const response = unwrap$(doResult);

        const [result$] = await m$.match<
            EventStream<shared.RichStream>,
            SDKError | SDKValidationError
        >(
            m$.sse(
                200,
                z.instanceof(ReadableStream<Uint8Array>).transform((stream) => {
                    return new EventStream({
                        stream,
                        decoder(rawEvent) {
                            const schema = shared.RichStream$inboundSchema;
                            return schema.parse(rawEvent);
                        },
                    });
                })
            ),
            m$.fail(["4XX", "5XX"])
        )(response);

        return unwrap$(result$);
    }

    async text(
        options?: RequestOptions & { serverURL?: string }
    ): Promise<EventStream<shared.TextEvent>> {
        const baseURL$ =
            options?.serverURL || pathToFunc(TextServerList[0], { charEncoding: "percent" })();

        const path$ = pathToFunc("/eventstreams/text")();

        const headers$ = new Headers({
            Accept: "text/event-stream",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "text",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = resolveGlobalSecurity(security$);

        const requestRes$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );
        const request$ = unwrap$(requestRes$);

        const doResult = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });
        const response = unwrap$(doResult);

        const [result$] = await m$.match<
            EventStream<shared.TextEvent>,
            SDKError | SDKValidationError
        >(
            m$.sse(
                200,
                z.instanceof(ReadableStream<Uint8Array>).transform((stream) => {
                    return new EventStream({
                        stream,
                        decoder(rawEvent) {
                            const schema = shared.TextEvent$inboundSchema;
                            return schema.parse(rawEvent);
                        },
                    });
                })
            ),
            m$.fail(["4XX", "5XX"])
        )(response);

        return unwrap$(result$);
    }
}
