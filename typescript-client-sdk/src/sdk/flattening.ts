/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
    encodeFormQuery as encodeFormQuery$,
    encodeJSON as encodeJSON$,
    encodeSimple as encodeSimple$,
} from "../lib/encodings.js";
import * as m$ from "../lib/matchers.js";
import * as schemas$ from "../lib/schemas.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import { extractSecurity, resolveGlobalSecurity } from "../lib/security.js";
import { pathToFunc } from "../lib/url.js";
import { SDKError } from "./models/errors/sdkerror.js";
import { SDKValidationError } from "./models/errors/sdkvalidationerror.js";
import * as operations from "./models/operations/index.js";
import * as shared from "./models/shared/index.js";
import { unwrap as unwrap$ } from "./types/fp.js";

export class Flattening extends ClientSDK {
    async componentBodyAndParamConflict(
        simpleObject: shared.SimpleObject,
        str: string,
        options?: RequestOptions
    ): Promise<operations.ComponentBodyAndParamConflictRes> {
        const input$: operations.ComponentBodyAndParamConflictRequest = {
            simpleObject: simpleObject,
            str: str,
        };

        const parsed$ = schemas$.safeParse(
            input$,
            (value$) =>
                operations.ComponentBodyAndParamConflictRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const payload$ = unwrap$(parsed$);
        const body$ = encodeJSON$("body", payload$.simpleObject, { explode: true });

        const path$ = pathToFunc("/anything/flattening/componentBodyAndParamConflict")();

        const query$ = encodeFormQuery$({
            str: payload$.str,
        });

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "componentBodyAndParamConflict",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = resolveGlobalSecurity(security$);

        const requestRes$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );
        const request$ = unwrap$(requestRes$);

        const doResult = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });
        const response = unwrap$(doResult);

        const [result$] = await m$.match<
            operations.ComponentBodyAndParamConflictRes,
            SDKError | SDKValidationError
        >(
            m$.json(200, operations.ComponentBodyAndParamConflictRes$inboundSchema),
            m$.fail(["4XX", "5XX"])
        )(response);

        return unwrap$(result$);
    }

    async componentBodyAndParamNoConflict(
        paramStr: string,
        simpleObject: shared.SimpleObject,
        options?: RequestOptions
    ): Promise<operations.ComponentBodyAndParamNoConflictRes> {
        const input$: operations.ComponentBodyAndParamNoConflictRequest = {
            paramStr: paramStr,
            simpleObject: simpleObject,
        };

        const parsed$ = schemas$.safeParse(
            input$,
            (value$) =>
                operations.ComponentBodyAndParamNoConflictRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const payload$ = unwrap$(parsed$);
        const body$ = encodeJSON$("body", payload$.simpleObject, { explode: true });

        const path$ = pathToFunc("/anything/flattening/componentBodyAndParamNoConflict")();

        const query$ = encodeFormQuery$({
            paramStr: payload$.paramStr,
        });

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "componentBodyAndParamNoConflict",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = resolveGlobalSecurity(security$);

        const requestRes$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );
        const request$ = unwrap$(requestRes$);

        const doResult = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });
        const response = unwrap$(doResult);

        const [result$] = await m$.match<
            operations.ComponentBodyAndParamNoConflictRes,
            SDKError | SDKValidationError
        >(
            m$.json(200, operations.ComponentBodyAndParamNoConflictRes$inboundSchema),
            m$.fail(["4XX", "5XX"])
        )(response);

        return unwrap$(result$);
    }

    async conflictingParams(
        strPathParameter: string,
        strQueryParameter: string,
        options?: RequestOptions
    ): Promise<operations.ConflictingParamsRes> {
        const input$: operations.ConflictingParamsRequest = {
            strPathParameter: strPathParameter,
            strQueryParameter: strQueryParameter,
        };

        const parsed$ = schemas$.safeParse(
            input$,
            (value$) => operations.ConflictingParamsRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const payload$ = unwrap$(parsed$);
        const body$ = null;

        const pathParams$ = {
            str: encodeSimple$("str", payload$.strPathParameter, {
                explode: false,
                charEncoding: "percent",
            }),
        };

        const path$ = pathToFunc("/anything/flattening/conflictingParams/{str}")(pathParams$);

        const query$ = encodeFormQuery$({
            str: payload$.strQueryParameter,
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "conflictingParams",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = resolveGlobalSecurity(security$);

        const requestRes$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );
        const request$ = unwrap$(requestRes$);

        const doResult = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });
        const response = unwrap$(doResult);

        const [result$] = await m$.match<
            operations.ConflictingParamsRes,
            SDKError | SDKValidationError
        >(
            m$.json(200, operations.ConflictingParamsRes$inboundSchema),
            m$.fail(["4XX", "5XX"])
        )(response);

        return unwrap$(result$);
    }

    async inlineBodyAndParamConflict(
        requestBody: operations.InlineBodyAndParamConflictRequestBody,
        str: string,
        options?: RequestOptions
    ): Promise<operations.InlineBodyAndParamConflictRes> {
        const input$: operations.InlineBodyAndParamConflictRequest = {
            requestBody: requestBody,
            str: str,
        };

        const parsed$ = schemas$.safeParse(
            input$,
            (value$) => operations.InlineBodyAndParamConflictRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const payload$ = unwrap$(parsed$);
        const body$ = encodeJSON$("body", payload$.RequestBody, { explode: true });

        const path$ = pathToFunc("/anything/flattening/inlineBodyAndParamConflict")();

        const query$ = encodeFormQuery$({
            str: payload$.str,
        });

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "inlineBodyAndParamConflict",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = resolveGlobalSecurity(security$);

        const requestRes$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );
        const request$ = unwrap$(requestRes$);

        const doResult = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });
        const response = unwrap$(doResult);

        const [result$] = await m$.match<
            operations.InlineBodyAndParamConflictRes,
            SDKError | SDKValidationError
        >(
            m$.json(200, operations.InlineBodyAndParamConflictRes$inboundSchema),
            m$.fail(["4XX", "5XX"])
        )(response);

        return unwrap$(result$);
    }

    async inlineBodyAndParamNoConflict(
        requestBody: operations.InlineBodyAndParamNoConflictRequestBody,
        paramStr: string,
        options?: RequestOptions
    ): Promise<operations.InlineBodyAndParamNoConflictRes> {
        const input$: operations.InlineBodyAndParamNoConflictRequest = {
            requestBody: requestBody,
            paramStr: paramStr,
        };

        const parsed$ = schemas$.safeParse(
            input$,
            (value$) => operations.InlineBodyAndParamNoConflictRequest$outboundSchema.parse(value$),
            "Input validation failed"
        );
        const payload$ = unwrap$(parsed$);
        const body$ = encodeJSON$("body", payload$.RequestBody, { explode: true });

        const path$ = pathToFunc("/anything/flattening/inlineBodyAndParamNoConflict")();

        const query$ = encodeFormQuery$({
            paramStr: payload$.paramStr,
        });

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ = await extractSecurity(this.options$.security);
        const context = {
            operationID: "inlineBodyAndParamNoConflict",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = resolveGlobalSecurity(security$);

        const requestRes$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                uaHeader: "x-speakeasy-user-agent",
                timeoutMs: options?.timeoutMs || this.options$.timeoutMs || -1,
            },
            options
        );
        const request$ = unwrap$(requestRes$);

        const doResult = await this.do$(request$, {
            context,
            errorCodes: ["4XX", "5XX"],
            retryConfig: options?.retries || this.options$.retryConfig,
            retryCodes: options?.retryCodes || ["429", "500", "502", "503", "504"],
        });
        const response = unwrap$(doResult);

        const [result$] = await m$.match<
            operations.InlineBodyAndParamNoConflictRes,
            SDKError | SDKValidationError
        >(
            m$.json(200, operations.InlineBodyAndParamNoConflictRes$inboundSchema),
            m$.fail(["4XX", "5XX"])
        )(response);

        return unwrap$(result$);
    }
}
