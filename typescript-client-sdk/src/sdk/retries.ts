/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { SDKHooks } from "../hooks/hooks.js";
import { SDKOptions, serverURLFromOptions } from "../lib/config.js";
import {
    encodeFormQuery as encodeFormQuery$,
    encodeJSON as encodeJSON$,
} from "../lib/encodings.js";
import { HTTPClient } from "../lib/http.js";
import * as retries$ from "../lib/retries.js";
import * as schemas$ from "../lib/schemas.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as operations from "./models/operations/index.js";
import { RetriesAfterServerList } from "./models/operations/retriesafter.js";
import { RetriesConnectErrorGetServerList } from "./models/operations/retriesconnecterrorget.js";
import { RetriesFlatEmptyResponsePostServerList } from "./models/operations/retriesflatemptyresponsepost.js";
import { RetriesGetServerList } from "./models/operations/retriesget.js";
import { RetriesPostServerList } from "./models/operations/retriespost.js";
import * as z from "zod";

export class Retries extends ClientSDK {
    private readonly options$: SDKOptions & { hooks?: SDKHooks };

    constructor(options: SDKOptions = {}) {
        const opt = options as unknown;
        let hooks: SDKHooks;
        if (
            typeof opt === "object" &&
            opt != null &&
            "hooks" in opt &&
            opt.hooks instanceof SDKHooks
        ) {
            hooks = opt.hooks;
        } else {
            hooks = new SDKHooks();
        }

        super({
            client: options.httpClient || new HTTPClient(),
            baseURL: serverURLFromOptions(options),
            hooks,
        });

        this.options$ = { ...options, hooks };
        void this.options$;
    }

    async retriesAfter(
        requestId: string,
        numRetries?: number | undefined,
        retryAfterVal?: number | undefined,
        options?: RequestOptions & { retries?: retries$.RetryConfig; serverURL?: string }
    ): Promise<operations.RetriesAfterRetries> {
        const input$: operations.RetriesAfterRequest = {
            numRetries: numRetries,
            requestId: requestId,
            retryAfterVal: retryAfterVal,
        };

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.RetriesAfterRequest$.outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(RetriesAfterServerList[0], { charEncoding: "percent" })();

        const path$ = this.templateURLComponent("/retries/after")();

        const query$ = encodeFormQuery$({
            "num-retries": payload$["num-retries"],
            "request-id": payload$["request-id"],
            "retry-after-val": payload$["retry-after-val"],
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ =
            typeof this.options$.security === "function"
                ? await this.options$.security()
                : this.options$.security;

        const context = {
            operationID: "retriesAfter",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                uaHeader: "x-speakeasy-user-agent",
            },
            options
        );

        const retryConfig = options?.retries ||
            this.options$.retryConfig || {
                strategy: "backoff",
                backoff: {
                    initialInterval: 10,
                    maxInterval: 200,
                    exponent: 1.5,
                    maxElapsedTime: 1000,
                },
                retryConnectionErrors: false,
            };

        const response = await retries$.retry(
            () => {
                const cloned = request$.clone();
                return this.do$(cloned, { context, errorCodes: ["4XX", "5XX"] });
            },
            { config: retryConfig, statusCodes: ["503"] }
        );

        const [result$] = await this.matcher<operations.RetriesAfterRetries>()
            .json(200, operations.RetriesAfterRetries$)
            .fail(["4XX", "5XX"])
            .match(response);

        return result$;
    }

    /**
     * A request to a non-valid port to test connection errors
     */
    async retriesConnectErrorGet(
        options?: RequestOptions & { retries?: retries$.RetryConfig; serverURL?: string }
    ): Promise<operations.RetriesConnectErrorGetRetries> {
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(RetriesConnectErrorGetServerList[0], {
                charEncoding: "percent",
            })();

        const path$ = this.templateURLComponent("/retriesConnectError")();

        const query$ = "";

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ =
            typeof this.options$.security === "function"
                ? await this.options$.security()
                : this.options$.security;

        const context = {
            operationID: "retriesConnectErrorGet",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                uaHeader: "x-speakeasy-user-agent",
            },
            options
        );

        const retryConfig = options?.retries ||
            this.options$.retryConfig || {
                strategy: "backoff",
                backoff: {
                    initialInterval: 500,
                    maxInterval: 60000,
                    exponent: 1.5,
                    maxElapsedTime: 3600000,
                },
                retryConnectionErrors: false,
            };

        const response = await retries$.retry(
            () => {
                const cloned = request$.clone();
                return this.do$(cloned, { context, errorCodes: ["4XX", "5XX"] });
            },
            { config: retryConfig, statusCodes: ["5XX"] }
        );

        const [result$] = await this.matcher<operations.RetriesConnectErrorGetRetries>()
            .json(200, operations.RetriesConnectErrorGetRetries$)
            .fail(["4XX", "5XX"])
            .match(response);

        return result$;
    }

    async retriesFlatEmptyResponsePost(
        requestId: string,
        requestBody?: operations.RetriesFlatEmptyResponsePostRequestBody | undefined,
        numRetries?: number | undefined,
        options?: RequestOptions & { retries?: retries$.RetryConfig; serverURL?: string }
    ): Promise<void> {
        const input$: operations.RetriesFlatEmptyResponsePostRequest = {
            requestBody: requestBody,
            numRetries: numRetries,
            requestId: requestId,
        };

        const payload$ = schemas$.parse(
            input$,
            (value$) =>
                operations.RetriesFlatEmptyResponsePostRequest$.outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$.RequestBody, { explode: true });
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(RetriesFlatEmptyResponsePostServerList[0], {
                charEncoding: "percent",
            })();

        const path$ = this.templateURLComponent("/retriesFlatEmptyResponse")();

        const query$ = encodeFormQuery$({
            "num-retries": payload$["num-retries"],
            "request-id": payload$["request-id"],
        });

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "*/*",
        });

        const security$ =
            typeof this.options$.security === "function"
                ? await this.options$.security()
                : this.options$.security;

        const context = {
            operationID: "retriesFlatEmptyResponsePost",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                uaHeader: "x-speakeasy-user-agent",
            },
            options
        );

        const retryConfig = options?.retries ||
            this.options$.retryConfig || {
                strategy: "backoff",
                backoff: {
                    initialInterval: 10,
                    maxInterval: 200,
                    exponent: 1.5,
                    maxElapsedTime: 1000,
                },
                retryConnectionErrors: false,
            };

        const response = await retries$.retry(
            () => {
                const cloned = request$.clone();
                return this.do$(cloned, { context, errorCodes: ["4XX", "5XX"] });
            },
            { config: retryConfig, statusCodes: ["503"] }
        );

        const [result$] = await this.matcher<void>()
            .void(200, z.void())
            .fail(["4XX", "5XX"])
            .match(response);

        return result$;
    }

    async retriesGet(
        requestId: string,
        numRetries?: number | undefined,
        options?: RequestOptions & { retries?: retries$.RetryConfig; serverURL?: string }
    ): Promise<operations.RetriesGetRetries> {
        const input$: operations.RetriesGetRequest = {
            numRetries: numRetries,
            requestId: requestId,
        };

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.RetriesGetRequest$.outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = null;
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(RetriesGetServerList[0], { charEncoding: "percent" })();

        const path$ = this.templateURLComponent("/retries")();

        const query$ = encodeFormQuery$({
            "num-retries": payload$["num-retries"],
            "request-id": payload$["request-id"],
        });

        const headers$ = new Headers({
            Accept: "application/json",
        });

        const security$ =
            typeof this.options$.security === "function"
                ? await this.options$.security()
                : this.options$.security;

        const context = {
            operationID: "retriesGet",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "GET",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                uaHeader: "x-speakeasy-user-agent",
            },
            options
        );

        const retryConfig = options?.retries ||
            this.options$.retryConfig || {
                strategy: "backoff",
                backoff: {
                    initialInterval: 10,
                    maxInterval: 200,
                    exponent: 1.5,
                    maxElapsedTime: 1000,
                },
                retryConnectionErrors: false,
            };

        const response = await retries$.retry(
            () => {
                const cloned = request$.clone();
                return this.do$(cloned, { context, errorCodes: ["4XX", "5XX"] });
            },
            { config: retryConfig, statusCodes: ["503"] }
        );

        const [result$] = await this.matcher<operations.RetriesGetRetries>()
            .json(200, operations.RetriesGetRetries$)
            .fail(["4XX", "5XX"])
            .match(response);

        return result$;
    }

    async retriesPost(
        requestId: string,
        requestBody?: operations.RetriesPostRequestBody | undefined,
        numRetries?: number | undefined,
        options?: RequestOptions & { retries?: retries$.RetryConfig; serverURL?: string }
    ): Promise<operations.RetriesPostRetries> {
        const input$: operations.RetriesPostRequest = {
            requestBody: requestBody,
            numRetries: numRetries,
            requestId: requestId,
        };

        const payload$ = schemas$.parse(
            input$,
            (value$) => operations.RetriesPostRequest$.outboundSchema.parse(value$),
            "Input validation failed"
        );
        const body$ = encodeJSON$("body", payload$.RequestBody, { explode: true });
        const baseURL$ =
            options?.serverURL ||
            this.templateURLComponent(RetriesPostServerList[0], { charEncoding: "percent" })();

        const path$ = this.templateURLComponent("/retries")();

        const query$ = encodeFormQuery$({
            "num-retries": payload$["num-retries"],
            "request-id": payload$["request-id"],
        });

        const headers$ = new Headers({
            "Content-Type": "application/json",
            Accept: "application/json",
        });

        const security$ =
            typeof this.options$.security === "function"
                ? await this.options$.security()
                : this.options$.security;

        const context = {
            operationID: "retriesPost",
            oAuth2Scopes: [],
            securitySource: this.options$.security,
        };
        const securitySettings$ = this.resolveGlobalSecurity(security$);

        const request$ = this.createRequest$(
            context,
            {
                security: securitySettings$,
                method: "POST",
                baseURL: baseURL$,
                path: path$,
                headers: headers$,
                query: query$,
                body: body$,
                uaHeader: "x-speakeasy-user-agent",
            },
            options
        );

        const retryConfig = options?.retries ||
            this.options$.retryConfig || {
                strategy: "backoff",
                backoff: {
                    initialInterval: 10,
                    maxInterval: 200,
                    exponent: 1.5,
                    maxElapsedTime: 1000,
                },
                retryConnectionErrors: false,
            };

        const response = await retries$.retry(
            () => {
                const cloned = request$.clone();
                return this.do$(cloned, { context, errorCodes: ["4XX", "5XX"] });
            },
            { config: retryConfig, statusCodes: ["503"] }
        );

        const [result$] = await this.matcher<operations.RetriesPostRetries>()
            .json(200, operations.RetriesPostRetries$)
            .fail(["4XX", "5XX"])
            .match(response);

        return result$;
    }
}
